import 'package:flutter/material.dart';
import 'package:fairshare/components/home/home.dart';

class Portrait extends StatelessWidget {
  const Portrait({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
      
      ],
    );
  }
}
